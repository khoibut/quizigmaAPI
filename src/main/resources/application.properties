# Application name
spring.application.name=quizigma

# PostgreSQL database configuration
# Uses DATABASE_URL for Render, localhost for local development
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/quizigma}
spring.datasource.username=${DB_USERNAME:khoibut}
spring.datasource.password=${DB_PASSWORD:0934134837}

# Hibernate JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Spring Security
jwt.secret=${SECRET}
